quarkus:
  hibernate-orm:
    database:
      default-schema: mystays
      generation: update
    dialect: org.hibernate.dialect.PostgreSQL81Dialect
    log:
      sql: 'true'
  datasource:
    username: postgres
    password: manager123
    jdbc:
      url: jdbc:postgresql://localhost:5432/postgres?currentSchema=mystays;
    

